[package]
name = "proyecto-ray-caster"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 3
debug = false

[dependencies]
rand = "0.8.5"
raylib = { version = "6.0.0", features = ["SUPPORT_IMAGE_GENERATION"] }